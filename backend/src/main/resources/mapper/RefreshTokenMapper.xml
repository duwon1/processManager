<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.processmanager.mapper.RefreshTokenMapper">

    <!-- 유저당 1개만 유지: 교체 시 이전 토큰 정보를 Grace Period용으로 보존 -->
    <insert id="upsert" parameterType="com.example.processmanager.entity.RefreshToken">
        INSERT INTO refresh_tokens (user_email, token_hash, salt, expires_at)
        VALUES (#{userEmail}, #{tokenHash}, #{salt}, #{expiresAt})
        ON DUPLICATE KEY UPDATE
            prev_token_hash = token_hash,
            prev_salt       = salt,
            replaced_at     = NOW(),
            token_hash      = #{tokenHash},
            salt            = #{salt},
            expires_at      = #{expiresAt}
    </insert>

    <!-- 이메일로 토큰 조회 (Grace Period 필드 포함) -->
    <select id="findByUserEmail" parameterType="String"
            resultType="com.example.processmanager.entity.RefreshToken">
        SELECT id, user_email, token_hash, salt, prev_token_hash, prev_salt, replaced_at, expires_at, created_at
        FROM refresh_tokens
        WHERE user_email = #{userEmail}
    </select>

    <!-- 로그아웃 또는 토큰 폐기 -->
    <delete id="deleteByUserEmail">
        DELETE FROM refresh_tokens WHERE user_email = #{userEmail}
    </delete>

</mapper>
