# TODO - Process Manager

## 현재 우선순위

### 1. 노드 인증/설치 안정화
- [x] 계정 토큰을 신규 등록용으로 분리
- [x] 노드별 `agent_secret` 발급 및 해시 저장
- [x] 에이전트가 `AGENT_SECRET`을 `.env`에 저장하고 이후 재접속에 사용
- [x] 토큰 재발급 시 이전 토큰으로 신규 등록 불가 처리
- [x] 개발/운영 에이전트를 한 PC에 동시에 설치 가능하도록 인스턴스 경로/서비스명 분리
- [x] 개발/운영 에이전트 포트 충돌 방지
- [x] 에이전트 `.env` 파일 권한 설정 (`chmod 600`)
- [ ] 운영 에이전트 재설치 후 `agent_secret` 발급/저장 확인
- [ ] 기존 설치 에이전트가 새 secret 구조로 정상 전환되는지 검증
- [ ] 노드별 `agent_secret` 폐기 기능
- [ ] 노드별 `agent_secret` 회전 기능
- [ ] 삭제된 노드의 secret 즉시 무효화 정책 정리
- [ ] 에이전트 GitHub 저장소에 설치 스크립트 런타임 패치 내용을 정식 반영

### 2. 노드 삭제 안정화
- [x] 삭제 요청 시 노드를 삭제 대기 상태로 전환
- [x] 에이전트 uninstall ACK 수신 후 최종 삭제
- [x] 구버전 에이전트 ACK 누락 시 유예 후 정리
- [x] 오래된 삭제 예약 자동 정리
- [ ] 운영 환경에서 삭제 대기/ACK/최종 삭제 흐름 재검증
- [ ] 오프라인 노드 삭제 정책 명확화
- [ ] 삭제 실패 사유를 프론트에 상태로 표시

### 3. 서비스 관리 탭
- [x] 에이전트: `systemctl list-units --type=service` 기반 서비스 목록 수집
- [x] 에이전트: 서비스 start / stop / restart / enable / disable 명령 처리
- [x] 백엔드: 서비스 목록 STOMP 채널 추가
- [x] 백엔드: 서비스 제어 명령 라우팅
- [x] 프론트엔드: 서비스 탭 UI
- [ ] 에이전트: sudoers 권한을 `/bin/systemctl` 범위로 축소 검토
- [ ] 서비스 제어 결과 실패 메시지 UX 개선

### 4. 테스트/배포 안정화
- [x] 테스트 전용 H2 설정 추가
- [x] SSH 터널 없는 테스트/운영 직접 DB 연결 지원
- [x] WebSocket 포함 Spring Boot context test 통과
- [x] Fly 배포 전 로컬 프론트 lint/build 확인
- [x] Fly 배포 전 백엔드 Gradle build 확인
- [ ] 배포 후 DB 마이그레이션 상태 자동 점검 스크립트
- [ ] 배포 후 노드 WebSocket 연결 상태 자동 점검

---

## 기능 확장 로드맵

### Docker 컨테이너 관리
- [ ] 에이전트: `docker ps -a` 기반 컨테이너 목록 수집
- [ ] 에이전트: 컨테이너 start / stop / restart / remove 명령
- [ ] 에이전트: `docker logs --tail` 로그 조회
- [ ] 에이전트: 컨테이너 리소스 사용량 (`docker stats`)
- [ ] 백엔드: Docker 관련 STOMP 채널 추가
- [ ] 프론트엔드: Docker 탭 UI (컨테이너 목록 + 상태 + 로그 뷰어 + 제어 버튼)

### 파일 관리자
- [ ] 에이전트: 디렉토리 목록 조회 API
- [ ] 에이전트: 파일 읽기 / 쓰기 / 삭제 / 이름변경 API
- [ ] 에이전트: 파일 업로드 / 다운로드 (청크 전송)
- [ ] 에이전트: 파일 권한 변경 (`chmod`)
- [ ] 백엔드: 파일 관련 STOMP 채널 및 REST API 추가
- [ ] 프론트엔드: 파일 탐색기 UI (트리뷰 + 파일 목록 + 텍스트 에디터)

### 알림 시스템
- [ ] DB: `alert_rules` 테이블
- [ ] 백엔드: 모니터링 데이터 임계값 초과 감지
- [ ] 백엔드: 알림 발송 (이메일 / 디스코드 웹훅 / 슬랙 웹훅)
- [ ] 백엔드: 알림 이력 저장
- [ ] 프론트엔드: 알림 규칙 설정 UI
- [ ] 프론트엔드: 실시간 알림 토스트 + 벨 아이콘 + 알림 이력 조회

### 로그 뷰어
- [ ] 에이전트: 로그 파일 목록 조회 (`/var/log` 스캔)
- [ ] 에이전트: 로그 실시간 tail (WebSocket 스트리밍)
- [ ] 에이전트: 로그 검색 / 필터링 (`grep`)
- [ ] 백엔드: 로그 스트리밍 STOMP 채널 추가
- [ ] 프론트엔드: 로그 뷰어 UI (파일 선택 + 실시간 스트리밍 + 검색/필터)

### 다중 노드 대시보드
- [ ] 프론트엔드: 전체 노드 한눈에 보는 개요 페이지
- [ ] 프론트엔드: 노드별 CPU/메모리/디스크 상태 카드 그리드
- [ ] 프론트엔드: 노드 상태 이상 시 하이라이트 표시
- [ ] 백엔드: 전체 노드 최신 메트릭 집계 API

---

## 기타 개선사항

### 팀 관리
- [ ] DB: `teams`, `team_members`, `team_nodes` 테이블 설계
- [ ] 백엔드: 팀 CRUD API + 멤버 초대/제거
- [ ] 백엔드: 노드 공유 권한 체계
- [ ] 프론트엔드: 팀 생성/관리 UI

### 감사 로그
- [ ] DB: `audit_logs` 테이블
- [ ] 백엔드: 주요 액션 로깅 (kill, 서비스 제어, 로그인, 노드 등록/삭제 등)
- [ ] 프론트엔드: 감사 로그 조회 UI

### 기술 부채
- [ ] `ApiController` 도메인별 분리
- [ ] `DashBoard.jsx` WebSocket 로직 커스텀 훅 분리
- [ ] `ProcessTable.jsx` 서브 컴포넌트 분리
- [ ] `nodes.user_id` 인덱스 추가
- [ ] 에이전트 명령 브로드캐스트 구조를 노드별 타겟 채널로 개선
- [ ] 운영/개발 환경 설정 문서 정리
